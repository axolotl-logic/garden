A guarantee that if no new updates are made, eventually access of all nodes/replicas will return the same value ([[Strong eventual consistency]] and *furthermore* a pair of nodes having received the same updates will return the same value.

A common approach is [[Conflict-free replicated data type]].

#computer-science/backend 
#computer-science/theory 