formulas:
  Magic: ((file.size / 100) + file.backlinks.length *0.8 ).round(2)
views:
  - type: table
    name: Table
    filters:
      and:
        - file.folder == "notes"
    order:
      - formula.Magic
      - file.name
    sort: []
