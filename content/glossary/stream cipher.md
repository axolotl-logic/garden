A type of symetric cryptography algorithm that allows for encryption on the fly, byte by byte (or bit!)

Plaintext + random message generated from key + [[Outer/Initialization Vector (IV)]], one byte or bit at a time allowing for streaming
