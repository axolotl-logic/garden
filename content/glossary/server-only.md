A npm package that makes sure the file wherein it is imported is not sent to client.

The problem solved is that server code can be built into the client bundle unintentionally without causing an error.